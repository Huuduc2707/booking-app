import { EventCategoryEntity } from './event-category.entity';

describe('EventCategoryEntity', () => {
  it('should be defined', () => {
    expect(new EventCategoryEntity()).toBeDefined();
  });
});
