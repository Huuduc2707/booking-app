import React from 'react'
import { Box, Button, Modal } from '@mui/material';

const AddEventButton = () => {
  return (
    <div>
        <Button className='bg-green-500 text-md font-bold text-white' variant='contained'>
            Add Event
        </Button>
    </div>
  )
}

export default AddEventButton